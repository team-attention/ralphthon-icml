# Official VESSL Autoresearch A100 Run Card

## Source and branch

- Official cookbook commit: `97a0af14b0acae042162b1f70f17fbe2d570afa2`
- Karpathy source commit: `228791fb499afffb54b46200aca536f79142f117`
- Participant-owned fork URL:
- Dedicated remote branch (`autoresearch/<tag>`):
- Baseline full commit:
- Fork is writable and cloud-clonable:
- Official benchmark files verified unchanged:

Do not record Git credentials in this card.

## Research

- Campaign run tag:
- Planned trials (`baseline`, up to `candidate-3`, optional `winner-confirmation`):
- Falsifiable hypothesis:
- Baseline:
- Success rule (`val_bpb`, lower is better):
- Stop conditions:

## W&B upload approval

- Local synthetic offline tutorial passed:
- Entity:
- Project:
- Visibility:
- Group (`run_tag`):
- Job type (`autoresearch-trial`):
- Config/metric allowlist reviewed:
- Console, code, Git, requirements, machine metrics/info, dataset, checkpoint, and artifact uploads excluded:
- Unavoidable W&B SDK metadata disclosed (sanitized host, SDK/Python/platform versions, timestamps/runtime, SDK telemetry records):
- Offline run directories:
- Immediate directory-specific sync confirmation recorded outside this file:

Never record credentials in this card or place a W&B secret in VESSL.

## VESSL resource approval

- Organization / team:
- Cluster / region:
- Exact approved live resource-spec slug:
- Created Job resource-spec slug:
- Live resource-spec GPU identity and count (exactly one A100):
- Structured GPU count (`1`):
- Approved and created Job resource specs are identical:
- Container image:
- Live hourly price:
- Available credit:
- Object-volume slug / mount:
- Storage capacity and hourly price:
- Expected Job duration / wall-clock cap:
- Estimated compute and storage cost:
- Timeout value and approved timeout action:
- Normal cleanup plan:
- Explicit creation confirmation recorded outside this file:

## Frozen identities

- Canonical cache fingerprint (`sha256:<64hex>`, sorted `data/` + `tokenizer/` content hash):
- Closing cache fingerprint matches:
- Evaluation fingerprint (`sha256:<64hex>` of pinned `prepare.py`):
- Baseline `train.py` SHA-256:

## Job correlation

Repeat this block for each trial in the append-only ledger:

- Trial:
- Full remote commit:
- `train.py` SHA-256:
- VESSL Job name:
- VESSL Job slug:
- VESSL terminal state:
- Saved local Job JSON:
- Saved local Job JSON SHA-256:
- Saved local log:
- Saved local log SHA-256:
- W&B offline run directory:

## Closeout

- Best confirmed `val_bpb`:
- A100/H100 comparison avoided:
- W&B sync destination and allowlist verified:
- Evidence paths:
- Jobs verified terminal:
- Job/Workspace Pause or Terminate choice and confirmation:
- Object-volume retention/deletion decision:
- Remaining storage or cost risk:
